export const TildeExplanation = `A tilde constraint allows patch-level changes if a minor version is specified. Allows minor-level changes if not.
`

export const CaretExplanation = `Caret constraints allow changes that do not modify the left-most non-zero element in the [major, minor, patch] tuple. In other words, this allows patch and minor updates for versions 1.0.0 and above, patch updates for versions 0.X >=0.1.0, and no updates for versions 0.0.X.
`

